One USER
    → owns zero or many DEVICEs

One DEVICE
    → has zero or many DEVICE_PUBLIC_KEYs

One USER
    → has zero or many SESSIONs

One DEVICE
    → may participate in zero or many SESSIONs

One SESSION
    → issues zero or many REFRESH_TOKENs

One USER
    → configures zero or many MFA_CONFIGURATIONs

One USER
    → receives zero or many MFA_CHALLENGEs

One SESSION
    → may require zero or many MFA_CHALLENGEs